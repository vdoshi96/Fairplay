# Gaps

- Repository integration tests fail locally because Prisma cannot reach a database server at `localhost:5432`.
- Migration application against a live Postgres database was not verified in this environment.
