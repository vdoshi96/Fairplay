# Blockers

## Active Blockers

- None.

## Watch Items

- Dark-mode fixes must be verified on actual React app pages, not only mocked visual smoke HTML.
- Global CSS dark overrides can hide component-specific color mistakes; screenshot QA must still inspect real pages.
- Greg avatar is generated as an original illustration, not an exact portrait or photo-real likeness.
- Little Alex idle behavior can fight Matter.js constraints if it applies large per-frame position changes.
- Little Alex customization adds a Prisma migration; generated Prisma client must be refreshed in each affected worktree.
- Final push must integrate any new remote commits instead of force pushing.
