# Learned

- `/Users/vishal/Developer/Fairplay` was not a git repository at task start.
- The workspace contained private/local materials in `References/`.
- The remote repository returned no symbolic `HEAD` refs, so `main` was used as the initial branch.
- GitHub CLI is installed and authenticated as `vdoshi96`.
- `.gitignore` excludes both `References/` and `.DS_Store`.
