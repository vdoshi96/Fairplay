# Learned

- The T01 scaffold is limited to the planned baseline surface: Next.js App Router, TypeScript, Tailwind CSS, ESLint, Vitest, Playwright, PWA metadata/manifest/icon routes, `.env.example`, root page, and implementation agent artifacts.
- `package.json` includes the required Node engine, scripts, and baseline dependencies for later tasks, including Prisma client/CLI, Zod, Argon2, nanoid, Testing Library, Vitest, and Playwright.
- The PWA baseline adds metadata, generated icon routes, and `manifest.ts` without adding a service worker or offline caching.
- The root page is a safe placeholder with neutral, original copy. It links toward future `/login` and `/create-household` entry points but does not implement auth or product workflows.
- The T01 range did not add production domain models, server code, Prisma schema, feature routes, seed content, starter catalogs, private-reference files, partner scores, clinical claims, or source-derived copy.

