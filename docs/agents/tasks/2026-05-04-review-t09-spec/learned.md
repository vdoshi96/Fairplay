# Learned

- The approved SVG placeholders in `public/assets/fairplay/` are exact copies of the approved originals in `docs/assets/visuals/`.
- `src/components/visuals/fairplay-visuals.tsx` centralizes default meaningful labels and decorative empty-alt handling.
- `src/app/globals.css` includes all requested visual tokens and the T09 motion hooks with a `prefers-reduced-motion: reduce` override.
- T09 explicitly documents the route-mocked e2e caveat in its `gaps.md` and `handoff.md`.
