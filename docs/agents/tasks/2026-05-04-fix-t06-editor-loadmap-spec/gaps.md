# Gaps

- The Playwright responsibility flow remains route-mocked and does not verify database-backed protected app behavior.
- The load-map radar filter now uses linked radar item ids/state, but richer radar workflows remain T07 scope.
- Visibility changes are submitted through a dedicated path, but the editor does not yet show a persistence state or inline API error message.
