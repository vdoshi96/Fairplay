# Learned

- T10 is documentation-only and did not modify feature implementation files.
- `package.json` scripts match the commands documented in the README and deployment docs.
- The app currently relies on Vercel default Next.js detection; no custom Vercel configuration file is necessary for T10.
- Full Vitest includes DB-backed repository integration coverage that requires a local or managed Postgres database.
