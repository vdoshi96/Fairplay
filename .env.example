# Local Docker Postgres from compose.yaml:
DATABASE_URL="postgresql://fairplay:fairplay_local_password@localhost:5432/fairplay?schema=public"

# Keep real managed database credentials outside source control.
SESSION_SECRET="replace-with-a-long-random-secret"
AUTH_COOKIE_NAME=fairplay_session
APP_BASE_URL=http://localhost:3000
